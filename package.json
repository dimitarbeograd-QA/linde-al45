{
  "name": "linde-al45",
  "private": true,
  "version": "1.0.0",
  "description": "Linde AL45 — интерактивна схема с части (E2E тестове)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
